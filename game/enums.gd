extends Object

class_name Enums

# GUI
enum Mode { SELECT, BUILD }

# PAWNS
enum Race { DWARF, ELF, HUMAN, ORC, VOIDBORN }
enum Stats { MIGHT, WILL, MAGIC }

# JOBS
enum Jobs { MOVE, BUILD, HAUL }

# BUILDINGS
enum Buildings { WALL, DOOR }

# ITEMS
enum Items { LUMBER }

extends Object

class_name Enums

# GUI
enum Mode { SELECT, BUILD, CHOP }

# PAWNS
enum Race { DWARF, ELF, HUMAN, ORC, VOIDBORN }
enum Stats { MIGHT, WILL, MAGIC }

# JOBS
enum Jobs { MOVE, BUILD, HAUL, CHOP, FORAGE, SOW, REAP, CULTIVATE, MINE, FORGE }

# BUILDINGS
enum Buildings { WALL, DOOR }

# ITEMS
enum Items { LUMBER }
